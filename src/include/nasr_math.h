#ifndef NASR_MATH_H
#define NASR_MATH_H

#define NASR_MATH_MAX( a, b ) (((a) > (b)) ? (a) : (b))
#define NASR_MATH_MIN( a, b ) (((a) < (b)) ? (a) : (b))

int NasrMathIsPrime( int n );

#endif // NASR_MATH_H